

module Math.SLHS.Operators
       ( add
       ) where

import Math.SLHS.Core
import Math.SLHS.Opinions


add :: SLExpr (SLState a) (Binomial a)
    -> SLExpr (SLState a) (Binomial a)
    -> SLExpr (SLState a) (Binomial a)
add = undefined
